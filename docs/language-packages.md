# Language Packages

Each language package contains a manifest, lexemes, senses, forms and
paradigms. The package loader validates IDs, references, hashes and package
identity before creating a `LanguageModel`. Surface forms are data, not Rust
conditionals. Feature structures select compatible forms and report conflicts
as typed errors.
